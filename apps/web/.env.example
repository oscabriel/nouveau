# Public build-time config for the web frontend. Safe to commit values:
# everything here is baked into the browser bundle. Never put secrets here —
# the file itself is gitignored; this is a template for fresh clones.
VITE_CONVEX_URL=https://api.nouveau.coffee
