# Hackathon log

- **Project:** nouveau
- **Event:** Convex All Gas Hackathon
- **What it does:** A coffee memory for home brewers. Log the lots you tried, ask Find my next bag for a shortlist of coffees in stock right now (OpenAI compares them to your logs using the roaster's own words, fetched with Firecrawl), save the ones you want to try, and watch roasters so AgentMail emails you when a new lot drops, one comes back in stock, or a price falls.
- **Demo:** _video link to be added at submission (Task 6)_
- **Live app:** https://artful-chameleon-402.convex.site
- **Repo:** https://github.com/oscabriel/nouveau
- **Frontend:** Convex static hosting
- **Convex deployment:** https://artful-chameleon-402.convex.cloud
- **Components:** @agentmail/convex, @convex-dev/aggregate, @convex-dev/auth (core + Google OAuth), @convex-dev/rate-limiter, @convex-dev/static-hosting, @convex-dev/workpool, @firecrawl/firecrawl-convex
- **Convex features:** schema, indexes, queries, mutations, actions, crons, scheduled functions, file storage, realtime queries, HTTP actions, workpool
- **Auth:** Convex Auth
- **AI models:** OpenAI `gpt-5.6-luna` (Responses API, low reasoning effort, strict JSON schema) for Find my next bag, live in prod at `/next-bag`. Firecrawl JSON extraction reads product pages for the same feature.
- **Started:** 2026-08-29T18:06:09Z
- **Last updated:** 2026-09-15T23:00:00Z

## Log

### 2026-08-29 - 4be892a

Scaffolded with Better-T-Stack: a Turborepo monorepo (bun workspaces, catalog deps) with a TanStack Router + Tailwind web app (`apps/web/`), shared `ui`/`env`/`config` packages, and a Convex backend (`packages/backend/convex/`), linted by ultracite. Template demo so far: a `todos` table with a list query and create/toggle/delete mutations wired to the frontend with realtime `useQuery`/`useMutation`, plus a health-check query. Convex features: schema, queries, mutations, realtime queries (`packages/backend/convex/schema.ts`, `packages/backend/convex/todos.ts`, `apps/web/src/routes/todos.tsx`).

### 2026-08-29 - working tree

Set up the agent environment. Copied in the project `AGENTS.md` and `.agents/` tooling (Convex guidelines docs, GitHub issue-tracker docs, and the `convex-hackathon-skill` build-log skill). Installed the `pi-mcp-adapter` package and configured the Convex MCP server (`~/.config/mcp/mcp.json`, `npx convex mcp start`); active after the next pi restart. Convex AI files in `packages/backend` and global Convex skills were installed and then removed in favor of the project-local `.agents/` docs and skills. No Convex deployment or hosting component yet; frontend target is Convex static hosting.

### 2026-08-29 - d554214

Toolchain pass: upgraded TypeScript to 7.0.2 (the native compiler), ultracite to 7.10.7 with oxfmt 0.65, and refreshed all workspace deps. Adopted the new function-style lint rules across app code (arrow-function components, hoisted route definitions, no nested ternaries) and updated the oxc configs to exclude shadcn UI components from linting. No product behavior change (`package.json`, `oxlint.config.ts`, `oxfmt.config.ts`).

### 2026-08-29 - a37561b

Deployed to production on Convex static hosting. Registered the `@convex-dev/static-hosting` component (owns `/`, app HTTP endpoints reserved under `/api`), added a root `convex.json` so the Convex CLI and MCP server work from the repo root, and pinned the deploy script to the prod deployment. First build is live at https://artful-chameleon-402.convex.site with the backend at https://artful-chameleon-402.convex.cloud; the health-check query verified against prod. Components: @convex-dev/static-hosting (`packages/backend/convex/convex.config.ts`).

### 2026-08-31 - working tree

Wayfinding pass complete: locked the full product shape through the wayfinder map (GitHub Issues #1–#9) — auth (Google OAuth on @convex-dev/auth 0.0.95), seed list (20 verified US Shopify roasters), sponsor component pins (firecrawl 0.1.1, agentmail 0.1.0), the launch data model (10 tables, baseline-crawl and 3-strike archive rules, email-ledger notifications), coverage flows (user submissions with quotas, local scenes), and the notification/feed UX (global + personalized live feeds, locked alert email template, per-user AgentMail inboxes, watch-status chips, drop-rhythm prediction card). Wrote the synthesized locked build spec (`.agents/docs/build-spec.md`, uncommitted) plus `CONTEXT.md` glossary and ADR-0001 (Shopify /products.json as primary extraction). Set sponsor env keys (Firecrawl, OpenAI, AgentMail) on the dev deployment. No product code yet; implementation starts with schema + seed data.

### 2026-08-31 - working tree (schema + auth + seed)

Implemented the launch schema in `packages/backend/convex/schema.ts`: all ten locked tables (roasters, crawlSources, products, productVariants, dropEvents, watches, notifications, localScenes, rawCaptures, users) with the locked indexes and the authTables spread from @convex-dev/auth. Wired launch auth: Google OAuth only (`convex/auth.ts`, `convex/auth.config.ts`, HTTP routes at `convex/http.ts`), pinned @convex-dev/auth 0.0.95 + @auth/core 0.41.1; set JWT_PRIVATE_KEY/JWKS/SITE_URL on the dev deployment. Mounted @convex-dev/aggregate and @convex-dev/rate-limiter components. Seeded all 20 verified US roasters + crawlSources rows on dev via `convex/seed.ts` (idempotent internal mutation; roasters enter `pending`, flip to `active` on baseline crawl). Removed the scaffold todos module (backend + web route). Auth env keys AUTH_GOOGLE_ID/SECRET pending (user holds the Google OAuth apps). Components: @convex-dev/aggregate, @convex-dev/rate-limiter; Convex features: schema, scheduled mutations (planned), file storage (planned).

### 2026-09-01 - working tree (Convex Auth v2 + verified Google sign-in)

Migrated auth to Convex Auth v2 (@convex-dev/auth 2.0.0-alpha.1). The auth core and oauthGoogle components are mounted in `convex.config.ts` (JWKS at `/auth/.well-known/jwks.json`, Google callback at `/oauth/google/callback`), the app owns its `users` table outright (`convex/users.ts`, createUser deduped by providerAccountId), `auth.config.ts` uses a customJwt provider, and the v1 authTables spread is gone from the schema. Added Google sign-in/sign-out to the web header (`ConvexAuthProvider` with `api={api.auth}`). Verified the full sign-in round trip on the dev deployment — consent, callback, session, first users row — browsing through a stable HTTPS dev domain on a local Caddy reverse proxy, with the allowed redirect origin supplied by a SITE_URL deployment env var rather than hardcoded (`convex/auth.ts`, `apps/web/vite.config.ts`). Retired the v1 JWT_PRIVATE_KEY/JWKS deployment vars and closed issue #8 (sponsor + auth env keys all set). Components: @convex-dev/auth core + Google OAuth join aggregate, rate-limiter, and static-hosting.

### 2026-09-01 - working tree (extraction pipeline live on dev)

Built and verified the crawl/extraction pipeline (build order step 2). The Firecrawl component is mounted in `convex.config.ts`; a 5-minute cron tick claims due crawl sources and schedules crawler actions (`convex/crons.ts`, `convex/crawlSources.ts`). products_json sources fetch Shopify `/products.json` directly with Firecrawl scrape as bot-protection fallback, now walking paginated feeds past Shopify's 250-item page cap. Pagination alone took Sey Coffee from 250 truncated products to its full 887 (`convex/crawler.ts`, `convex/extraction.ts`). html-mode sources run a durable Firecrawl crawl with structured extraction, committed by an internal-mutation completion callback. Each crawl commits in one transaction: raw body capture to file storage, catalog upserts, drop-event diffing (baseline crawl fires none), the 3-strike archive, source health, and reschedule. Crons also sweep stale sources hourly and prune raw captures daily on a 3-day retention. Verified on the dev deployment: all 20 roasters active, and the first genuine drop event (a Sey variant selling out between crawls) recorded end to end. Convex features: actions, crons, scheduled functions, file storage. Component: @firecrawl/firecrawl-convex.

### 2026-09-02 - 12d8107

Hardened the extraction pipeline with a test suite and two live-found fixes (commits 7ed9ce9–12d8107). Added a convex-test + vitest suite (54 tests) over the pure extraction helpers and the crawl commit path: wholesale filter, Shopify pagination walk (raw feed count drives continuation; a lost page discards the catalog), baseline rule, all five drop-event types, variant-name dedupe, the 3-strike archive and resurrection, stale sweep, raw-capture prune, and the scheduler tick (`convex/extraction.test.ts`, `convex/crawlSources.test.ts`). Checking live health surfaced two defects: Proud Mary's 722-product / 5,500-variant catalog exceeded Convex's per-transaction read limit, so its crawls had failed silently for a day (the hourly stale sweep caught it) — the commit now runs as batched `applyProductBatch` mutations plus a `finalizeCrawl` step, driven from the crawler action; and Shopify Markets served Madcap's feed in AED at a floating rate, flapping 154 spurious price events, so every storefront fetch now pins the US market. Added `rebaselineSource` and `purgeRoasterEvents` operator mutations for repairing a source after a coverage or currency fix, and used them on dev: all 20 sources watching, 19 genuine drop events, Proud Mary and Madcap re-baselined in USD. Webhook-mode html crawls (Passenger) confirmed completing end to end via the Firecrawl component's completion callback. Convex features: actions, mutations, scheduled functions, file storage.

### 2026-09-02 - edfcd59

Shipped the watch and feed layer (build order step 3; commits 250997c–edfcd59). Signed-in users can watch and unwatch roasters and mute a watch; the mutations resolve the caller from the session identity in `convex/identity.ts` and never take a userId argument. Follower counts run through a @convex-dev/aggregate `TableAggregate` namespaced by roaster and update in the same transaction as every watch write (`convex/followerCounts.ts`, `convex/watches.ts`). Three feed queries return only alert-worthy events (new, back in stock, price drop), newest first, each card linking "See the lot" to the roaster's own product page: a global feed merged from one descending index scan per event type, a per-roaster drop history, and a personalized feed that joins the notifications ledger for a delivery footer (`convex/feed.ts`, new `by_type_and_detected_at` index). Watch status chips derive from crawl-source health with the locked copy ("Watching — last checked 4 min ago" / "Stale — …, still checking" / "Crawl failed — …") and appear on the directory, roaster page and watches page (`convex/health.ts`, `apps/web/src/components/status-chip.tsx`). New web screens: the signed-out home with the live global feed and a roaster teaser, the signed-in home with "Your roasters", delivery footers and an unhealthy-watch banner, plus `/feed`, `/roasters`, `/roasters/$slug` and `/watches`. 15 new convex-test cases bring the suite to 69. Verified live on the dev deployment: Google sign-in, browsing roasters, and adding a watch. Components: @convex-dev/aggregate. Convex features: queries, mutations, indexes, realtime queries.

### 2026-09-02 - 400f9a8

Shipped the alert email layer (build order step 4; commits 567b489–400f9a8). Mounted @agentmail/convex 0.1.0 in `convex.config.ts`; every Drop event now fans out inside the same transaction that emitted it to that roaster's unmuted watchers: one notifications-ledger row per (user, event) doubles as the one-email-per-event dedup guard (§5), and the email is enqueued from the mutation via the component's durable send (workpool retries) (`convex/notifications.ts`, fanout wired at all three event-emission sites in `convex/crawlSources.ts`). Emails render the locked §8.2 template as plain text — subject variants "New at / Back at / Price drop at", lead line, OpenAI tasting-note slot (renders `aiSummary` when present; generation deferred), grams · price · "See the lot" linking the roaster's own product page, roaster page link, and a mute footer (alert-settings link omitted until that stub exists). Per-user AgentMail inboxes (§8.3): signup schedules inbox provisioning from the auth createUser callback, the web header backfills users who predate the feature via a new `ensureInbox` mutation (`convex/users.ts`, `apps/web/src/components/header.tsx`, `agentmailInbox` on the users row), and alerts are sent from the user's own inbox to their email. The personalized feed's delivery footer now reads the component's reactive pending → sent → delivered lifecycle through the stored outboundId (bounced/complained/rejected map to failed). Mounted the AgentMail Svix webhook at `/api/agentmail/webhook` (`convex/http.ts`); it serves 500s on dev until `AGENTMAIL_WEBHOOK_SECRET` is set. 9 new convex-test cases (template, fanout, mute/inbox/dedup/type-gating, live status hydration) bring the suite to 78, with the component registered under its nested workpool paths. Pushed to dev `cool-giraffe-632` (feed query and webhook route verified live); prod still awaits the deploy. Components: @agentmail/convex. Convex features: HTTP actions, scheduled functions.

### 2026-09-03 - d4cf616

Debugged the alert inbox path on dev: `provisionInbox` died with "Couldn't resolve agentmail.lib.createInbox". Probing the deployed component showed public functions resolved while every `internal*` one didn't — Convex never exposes a component's internal functions to the parent app, and @agentmail/convex 0.1.0 built its whole client API on internal functions. Patched the package via bun patchedDependencies: the seven parent-called functions (createInbox, listInboxes, getInboxRemote, deleteInbox, listThreads, getThread, getMessage) become public actions, and the AGENTMAIL_* env vars are declared in `defineComponent` so the mount accepts them — the component reads its own namespaced env, so a deployment-level var alone is invisible to it. convex-test doesn't model that visibility boundary, so the 78-test suite stayed green until the live push caught it. The run then reached the AgentMail API and failed with 403 missing_permission: the dev key lacks inbox_create (broader key tracked on #13).

### 2026-09-04 - af813f0

First production deploy, live at https://artful-chameleon-402.convex.site. Added `scripts/deploy-prod.sh`, an eight-stage wizard that walks the human through the steps only they can do — a prod Google OAuth client, copying the per-account sponsor keys from dev, generating a fresh RS256 auth key pair, SITE_URL, `bun run deploy`, seeding, the prod AgentMail webhook, and a sign-in check — with every prod-affecting command announcing its target behind a confirm gate. Verified after: site 200, an unsigned webhook POST rejected 401, all 20 roasters seeded with 19 active after the baseline crawl (3,543 products), and the first real user signed in with Google and received their AgentMail inbox.

### 2026-09-04 - cdcb679

Fixed the first prod-found bug (issue #16): every fresh signup fired two concurrent `provisionInbox` actions — one scheduled by `createUser` at signup, one by `ensureInbox` at sign-in — and both passed the re-check inside the action, so the loser died with an AgentMail 403 on every signup. Inbox provisioning now serializes through a claim mutation: a claim timestamp on the users row decides the winner inside a transaction (mutations serialize; actions don't), the claim clears on success, releases on failure so the next sign-in retries immediately, and a 5-minute TTL retakes claims from crashed actions. Four new convex-test cases cover the race, claim expiry, failure release, and the already-provisioned no-op (suite at 82). Deployed to prod the same day. Both schedules still fire; the claim makes the second one a no-op, so the 403 should be gone, but no fresh signup has exercised it in prod yet.

### 2026-09-04 - 9442b9d

Reframed the pitch to "Letterboxd but for coffee" and shipped the social layer (build order step 6; commits 5575914–9442b9d, ADR-0002, build spec §14). Backend: a `logs` table (lot + optional 1–5 half-step rating + optional notes, ~1000 chars) with `by_logged_at` and `by_user_and_logged_at` indexes; `createLog`/`updateLog`/`deleteLog` resolve the author from the session and refuse edits to anyone else's row; `recentLogs` is the global activity feed (newest first, capped at 30); `profile` is the public page data (taster, logs, watched roasters); `roasters.listLots` pages a roaster's full catalog with archived lots left in since they stay loggable (`convex/logs.ts`, `convex/roasters.ts`, `convex/schema.ts`). Web: `/activity` (public recent-logs feed with half-step star displays), `/profile/$userId` (public profile; own logs get inline edit and delete), a Lots list on the roaster page with an inline log form, and header links (`apps/web/src/routes/activity.tsx`, `apps/web/src/routes/profile.$userId.tsx`, `apps/web/src/components/{lots,log-form,log-card,stars}.tsx`). 13 new convex-test cases, suite at 92. Live on dev; not yet deployed to prod. Roaster notes on lots (§14.4, the #14 re-scope) are still ahead. Convex features: reactive queries, pagination.

### 2026-09-09 - 965c862

Applied a two-axis code review of the social layer diff (af813f0..9442b9d) as one commit. Backend: logs.ts imports roasterCardValidator instead of duplicating its fields (the "import cycle" was a misdiagnosis — the real undefined-validator error was `v.array` over a bare field object, now wrapped in `v.object`); LogCard derives from the validator and the web from FunctionReturnType; profile takes the URL string and normalizes it so a malformed id resolves to the no-taster page instead of a validation error, returning logsTruncated so the count can't lie past the cap; lot discovery via a products.search_name search index and roasters.searchLots; log cards carry lot.url like the drop feed. Web: LogForm and LogCard delete get busy flags so double clicks can't duplicate logs. provisionInbox releases its claim only when the AgentMail call itself fails, since once the inbox exists remotely a release would mint a second one. 3 new tests (suite at 95), pushed to dev with the search index backfilled.

### 2026-09-09 - ecf4547

Shipped §14.4 roaster notes on lots (the #14 re-scope, ADR-0002). Extraction now captures what the roaster publishes on products.json: description (HTML stripped block-aware, so Ruby's `<h5>We Taste: …</h5>` lists stay whole), tags, and the first imageUrl; origin/process/roastLevel parse from observed tag conventions (Proud Mary `From:`/`Process:`, Intelligentsia `Country:`, Verve `Roast:`, Onyx `origin:`, Ruby bare `Washed`), roastLevel only when the value names an actual roast since Intelligentsia uses "Roast Level: Bright" for taste. roasterNotes is a regex over the roaster's own prose ("in the cup we find", "we taste:", "notes of", "flavors of") falling back to Intelligentsia's Flavor Profile tag, stored verbatim and null when nothing matches — descriptors are never invented, and OpenAI stays unused unless regex coverage proves poor ("in the cup, with a subtle pine-like character" narrative correctly rejects). Seven optional fields on `products`, written at upsert so the 3.5k-row catalog fills on the next crawl cycle with no migration; a feed that stops carrying a field leaves the stored value. Lot rows, the log form, and log cards show the descriptors as read-only reference the taster can crib from, never prefilled into personal notes (`convex/extraction.ts`, `convex/crawlSources.ts`, `convex/logs.ts`, `convex/roasters.ts`, `apps/web/src/components/{lots,log-form,log-card}.tsx`). 22 new convex-test cases bring the suite to 117 (extraction patterns incl. the Verve narrative case, upsert persistence across crawls, lot copy never emitting a drop event). Verified on dev: a live Proud Mary crawl filled origin "Ethiopia", process "Natural", and the note "jasmine, stone fruit, blackberry, & winey complexity" — words verbatim from their page. Convex features: full-text search (existing), batched upserts.

### 2026-09-09 - 19402aa

Applied a code review of the §14.4 diff as one commit. The two findings that mattered: stale roaster notes never cleared — lotCopy() only wrote fields the feed carried, so a roaster editing "notes of cherry" out of their copy left "cherry" on the lot forever — fixed by folding the seven flat optional fields into one optional `lotCopy` object where a present copy (products.json) is authoritative, every field written, `undefined` clears, and an absent copy (HTML mode) leaves the stored value alone; and the four notes regex lead-ins gained word boundaries after "Footnotes of the harvest" proved "notes of" would invent a note, violating the only-descriptors-literally-present rule. Also anchored ROAST_VALUE ("Roast: Lightly sweet" is not a roast level), bounded From: values with looksLikeOrigin, dropped `<script>`/`<style>` bodies before tag stripping, stopped LogCard rendering "Roaster notes:" twice while the form was open, and corrected the 965c862/ecf4547 entry dates to 2026-09-09. 5 new tests, suite at 122. Pushed to dev; a live Proud Mary crawl confirmed the changed applyProductBatch args shape against a real feed, health watching, notes still filling verbatim. Deliberately kept: capAtWord's strict `<` — the clause regexes cap at exactly 200 chars mid-word, so a text at the cap counts as truncated; a test enforces it.

### 2026-09-09 - working tree (prod deploy: social layer + lot copy)

Deployed the social layer and §14.4 lot copy to prod (`bun run deploy`, owner consent in session). Prod now runs the full diff since `cdcb679`: the logs/activity/profile layer, the search-driven lot discovery, and roaster-published lot copy (description, tags, imageUrl, origin/process/roastLevel, roasterNotes with clear-on-change) — the `products.search_name` index backfilled against the 3.5k-row catalog without blocking the deploy. Post-checks: site and `/activity` 200, healthCheck "OK", `logs:recentLogs` returns [] (nobody has logged yet), `roasters:searchLots` live on prod with the new lot shape (`roasterNotes: null` until prod crawls fill copy on their next cycle). Sign-in + log-a-lot round trip still to verify in a browser.

### 2026-09-10 - e4036bb

Every lot is now a page (build spec §15, ADR-0003, issue #18) — the owner's first-prod feedback was that logging required the detour through the roaster page and nothing named a lot linked anywhere in-product. `lots.get` is a public query keyed by the URL string (normalizeId, malformed ids resolve to null, same contract as `profile`), returning the lot's published copy (§14.4 fields), its roaster, and the lot's recent logs newest-first, capped at 20 with a truncation flag; new `logs.by_product_and_logged_at` index; log-card hydration exported from logs.ts rather than duplicated (`convex/lots.ts`, `convex/logs.ts`, `convex/schema.ts`). Web: `/lots/$lotId` shows the copy, attributes, roaster notes, a Log button (sign-in CTA signed out) and the lot's logs with inline edit/delete on your own; feed cards gain a Log action; log cards and roaster-page lot rows link the lot name to the lot page, with "See at {roaster}" keeping the shop link (`apps/web/src/routes/lots.$lotId.tsx`, `apps/web/src/components/{feed-card,log-card,lots}.tsx`). 5 new convex-test cases, suite at 127. Live on dev, verified against a real Proud Mary lot. Same session: filed #17 (trim non-coffee products — scales, teas, brewers — from extraction; the feed currently treats anything with a price as a lot) and #18 (this increment). Convex features: reactive queries, indexes.

### 2026-09-10 - df5b36e

Trimmed non-coffee products from extraction (build spec §16, issue #17). Sampled all 20 seed feeds (`/products.json`, 4,900 items) and built `classifyLot` in `convex/extraction.ts` from what they actually carry: signals in order, first decisive wins — wholesale; a title naming a non-lot format (subscription, bundle, sampler, gift set, k-cup/capsule/pod, latte/RTD, count-packs, bulk, add-on) or a hard good (scale, grinder, kettle, mug, tee, tote…), whatever the type says, because Counter Culture files gift subscriptions under `Coffee` and Proud Mary a Comandante grinder under `coffee-archive`; a tag whose exact value names a non-lot (`Coffee Type: Subscription Only`, `Shopify Collective`, `white-label`, `Equipment`), exact because Stumptown's real coffees carry `Filter: Subscription Eligible` and Counter Culture's a bare `subscription`; `product_type` per comma segment (Stumptown lists `Coffee/Africa,…,Gifts` on a real blend), with Coava's `Brewed Coffee` and PT's `Past Offerings Collection` recognised as coffee; then tags and title vocabulary for untyped items; default not-a-lot. Sold-out archives stay (a restock there is the drop). Along the way the original wholesale rule turned out to be a substring match that hid 48 of Sweet Bloom's 53 coffees (`wholesale-coffee` means "also sold wholesale"), plus Heart, Merit and Blossom retail bags; it now matches the type, the title (`- Wholesale`, `(WS)`, `- Cafe Only`) and exact tags (`wholesale-only`, `hide-from-retail`, `cafe-only`). Cleanup is part of the crawl: `parseProductsJson` reports rejected ids, `finalizeCrawl` deletes any still in the catalog with their variants and drop events, or archives one a taster has logged (§14.1: logs never lose their lot). 21 new convex-test cases (every classifier case is a real product title from a seed feed), suite at 148. Live on dev: one crawl pass took the catalog from 3,610 products to 2,553 and deleted 145 junk events (Draft Lattes, Hario kettles, Kimtech Wipes, Sunrise Rider Patch Hat); the newly visible retail coffees fired 283 `new` events that were catalog corrections, not drops, purged with `purgeRoasterEvents` — on prod, `rebaselineSource` for Sweet Bloom, Heart, Merit, Blossom, Onyx and Ruby must run right after the deploy and before their next crawl. Convex features: batched mutations, indexes.

### 2026-09-10 - a3995fe

Reviewed the classifier and fixed what the review caught (build spec §16, issue #17). First false negative found on real data: `cold brew`, `aeropress` and `chemex` sat in the pre-type title rules, so Blossom's whole-bean "Cold Brew Blend" (typed `Coffee`, 12 oz to 5 lb bags) was rejected outright — those words only decide untyped items now, moved into the untyped path next to `NON_LOT_UNTYPED_TITLE` ("Cold Brew Coffee 32oz" is still a bottle), and a new `NON_LOT_RTD_TITLE` catches the drinkable kind by its container (stubbies, nitro, keg, "4 Cans", "12oz Can") whatever the type says. The untyped path became `classifyUntyped`: a weak non-lot tag (`tea`, `recharge`) decides only when no tag says coffee (`tea` next to `Ethiopia` is a tasting note), and an ambiguous title word (`honey`, `cup`, `chocolate`) loses to a place or craft word in the same title ("Las Lajas Black Honey" is coffee, "Bird And The Bees Honey" is a jar). Two purge bugs: `purgeNonLot` and `purgeRoasterEvents` orphaned the notifications ledger rows of each deleted event — new `notifications.by_drop_event_id` index and a shared `deleteDropEvent` helper clear them — and the purge was uncapped, so a rule tightening on a Sey-sized catalog could blow the transaction; it now takes `PRUNE_BATCH` (200) products per crawl and the rest follow next crawl with no strike taken. Also: rejects with empty ids are no longer reported, `ge[i]?sha` → `gei?sha`. Regression check ran old vs new over 3,805 sampled real items: exactly one verdict flipped, the Cold Brew Blend. 7 new tests, suite at 155. Live on dev (`CONVEX_DEPLOYMENT=dev:cool-giraffe-632 npx convex dev --once`), verified by rebaselining Blossom and crawling it: the Cold Brew Blend is in the catalog as `current` with no `new` event, since a classifier fix is a catalog correction, not a drop. Convex features: indexes, batched mutations.

### 2026-09-10 - 23d77a0

Collapsed the per-variant `new` events to one per lot (build spec §5 rule 4, issue #19). `applyProductBatch` emitted a `new` event per variant, and the #17 crawl put 14 consecutive Proud Mary "Solo Daye Bensa" cards at the top of the dev feed — one per size, plus three for every Sey lot. A first sighting is one fact: `applyVariants` now emits a single `new` event citing the cheapest size (first minimum wins, so a price tie cites the size the feed listed first), while a size added to a known lot later keeps its own event and baseline crawls stay silent. The fan-out and the personalized feed's deliveryStatus join work per event, so the collapse also means one alert email per new lot instead of one per size. Two convex-test cases over the real commit path (`commitExtractedCatalog` → batches → `finalizeCrawl`), suite at 157; live on dev. Convex features: batched mutations, indexes.

### 2026-09-10 - b35dff6

Production is two commits ahead of where it woke up: lot pages (§15, `e4036bb`), the lot classifier (§16, `df5b36e` + review fixes `a3995fe`), and the new-event collapse (#19, `23d77a0`) are all live on `artful-chameleon-402` now, with both schema changes (`logs.by_product_and_logged_at`, `notifications.by_drop_event_id`) landing with the deploy. Deploy ran 15.7s. The #17 sequencing mattered and held: `rebaselineSource` ran for Sweet Bloom, Heart, Merit, Blossom, Onyx and Ruby within a minute of the deploy, so their first post-deploy crawls are silent baselines instead of ~280 spurious `new` cards from the newly visible retail coffees. Verified live: Blossom's baseline crawl put the review's confirmed false negative — "Cold Brew Blend", a whole-bean coffee the old pre-type rules rejected — into the prod catalog as `current`, with zero drop events fired and the global feed carrying only genuine pre-deploy events (PT's back-in-stock pair among them, per-variant on a known lot as designed). The junk purge (§16) proceeds on its own over the next crawls at the 200-per-roaster-per-crawl cap. Convex features: indexes, scheduled crawls.

### 2026-09-11 - c59f1c4

Re-planned against the All Gas criteria. A fit assessment graded the earlier draft and found that the app used Firecrawl for crawling and OpenAI for nothing at runtime, so the sponsor story was half true. The revised spec (`.agents/research/nouveau-reimagined-product-spec.md`) keeps the drop-alert and logging product and adds one grounded OpenAI feature behind gates: Gate 1, the existing flows verified on prod; Gate 2, Find my next bag with a real OpenAI call, output validation, private results and a demonstrated Firecrawl contribution; Gate 3, saving and watches on top. It also fixes rules the implementation had to follow: the model chooses existing IDs and copies supplied passages, never writes catalog facts, gets no tools, and every buying label comes from the database. A scope fallback names what ships if the shortlist misses its gates.

### 2026-09-12 - a08463d

Shipped Find my next bag (Gate 2). `/next-bag` takes a request in plain words, an optional USD cap and minimum bag size, and up to five of the user's own logs, with personal notes included only by checkbox; the page says what goes to OpenAI before the button. `recommendations.request` records the input, consumes per-user (5/hour) and global (30/hour) quotas, and queues `recommendationWorker.run` through a @convex-dev/workpool at concurrency two; the client subscribes to queued, running, ready or failed. Candidates come from crawl sources whose homepage confirmed `Shopify.country` US and `Shopify.currency.active` USD in the past hour (`convex/shopMarket.ts`, written into every crawl), taken round-robin across roasters so no large catalog fills the pool, each priced on one specific in-stock variant with a confirmed size. Firecrawl scrapes up to two product pages per request asking for markdown plus JSON extraction (process, variety, elevation, producer, roast level, tasting notes, description sentences); the backend keeps a value only when it appears in the page text and is absent from the Shopify feed's own copy for that lot, joins the facts under server-written labels, and caches the result 24 hours for every user. One OpenAI Responses call (`gpt-5.6-luna`, low effort, strict JSON schema, `store: false`, no tools) returns up to three existing product IDs, each with an evidence ID and a byte-for-byte copy of that passage; the server rejects unknown IDs, changed quotes and extra fields, drops any comparison sentence containing numbers, prices, stock, shipping or "you will" language, and rechecks the exact variant's stock, price and size before showing a buying link, again on every read. 62 convex-test cases (suite at 219) cover it (`convex/recommendation{s,Rules,Catalog,Worker}.ts`, `convex/recommendations.test.ts`, `apps/web/src/routes/next-bag.tsx`, `apps/web/src/components/recommendation-{form,results}.tsx`).

It took three live dev runs to make the Firecrawl contribution real, and `.agents/docs/recommendations.md` records each. Run one ("south american, natural process") returned three valid catalog quotes, but the two page scrapes yielded shop boilerplate, a markdown table and a `##` heading. Run two, after tighter regex filters, was worse in an instructive way: Verve's passages were image captions (`!Verve Coffee Roasters - Chelchele - Producer Image`) because link stripping ran before the image filter, and Onyx's were general coffee-education paragraphs. The captions actually held the useful data (`Process: Washed - Variety: Heirloom - Tasting Notes: Jasmine, Toffee, Lemon Custard`), which regexes over markdown could not pull out reliably, so extraction moved to Firecrawl's JSON format with the verbatim check above. Run three ("A washed Ethiopian coffee, floral and tea-like", 20 candidates from 13 roasters) quoted two Firecrawl fact lines the feed does not carry, Onyx `Variety: Landrace. Elevation: 1900 MASL. Roast level: Ultra Light. Tasting notes: Apricot, Barley Tea, Bergamot, Caramel.` and Verve `Variety: Heirloom. Elevation: 1900-2100 Meters. Producer: Smallholder outgrowers in the Chelchele kebele. Roast level: Light roast. Tasting notes: Jasmine, Toffee, Lemon Custard.`, and the model's comparison named "Barley Tea" and "Bergamot" as the echo of tea-like and floral. Between runs two and three the prompt gained a rule to prefer the most specific passage, and result cards now list unquoted page passages under "Also on the product page", so a scrape reaches the screen even when the model prefers catalog prose. Components: @convex-dev/workpool, @firecrawl/firecrawl-convex (scrape with JSON extraction), @convex-dev/rate-limiter. Convex features: workpool, scheduled watchdog, actions, realtime queries.

### 2026-09-12 - working tree (prod deploy: Find my next bag)

Deployed `a08463d` to prod (`bun run deploy`, owner consent in session, `OPENAI_API_KEY` set on `artful-chameleon-402` beforehand). The push carried the `recommendationRuns` and `recommendationEvidence` tables, the `products.by_roaster_and_status_and_last_seen_at` index, the `recommendationPool` workpool mount and the `/next-bag` route; 15.9s. Post-checks: `/` and `/next-bag` 200, `healthCheck` OK, 19 of 20 sources watching. No prod source carried a US/USD market confirmation at deploy time because that field is written by the crawl itself, and every source was due within 26 minutes, so the page answers "No eligible coffees" until those crawls land rather than treating an unconfirmed source as eligible. The prod evidence cache starts empty. The second-account privacy check was done on dev against the run recorded above.

### 2026-09-12 - 0fef273

Fixed the last catalog-quote embarrassment from run three (issue #21, part of #20). The East Pole card had quoted its spec table flattened into one line, `New Column` corner cells and all, because `catalogPassages` splits on sentence ends and a table stripped to one paragraph has none. A line that reads as a label run (three or more all-caps tokens of 4+ letters, or the literal `New Column`) is now mapped instead of quoted: known coffee headers (PRODUCER, ORIGIN/REGION, ALTITUDE/ELEVATION, VARIETY/VARIETAL, PROCESS, NOTES/TASTING NOTES, ROAST (LEVEL)) become the same server-written `Label: value.` facts the Firecrawl JSON path emits, values stay verbatim substrings of the description, AMOUNT and unknown headers drop with their values, shop prose glued to the last value ("We are thrilled to bring on this Washed Ethiopian!") is cut, and a run that maps to nothing stays out entirely — a flattened sheet is layout, not a sentence. The East Pole fixture now yields `Process: Washed. Variety: Heirloom. Region: Djimmaha Gera,Sadi Ioya Kebele. Elevation: 2,300 meters above sea level. Producer: Habtamu Gato. Tasting notes: Strawberry, Lemonade, Brown Sugar.` The §14.4 extractor emitting a newline per `<tr>` (so tables never flatten in the first place) remains open as a separate change with its own crawl-side effects. One new convex-test case over the real East Pole text (suite at 220); live on dev (`convex dev --once`, 2.9s, no schema change); no `recommendationEvidence` purge needed since the cache holds page passages only and catalog passages are rebuilt from the stored description per request.

### 2026-09-12 - a959b09

Replayed the change through real dev data instead of trusting the fixture, and it caught a gap (issue #21). East Pole's live "Traffic" description flattens the spec table and the next sentence into one paragraph with no punctuation between them (`...Sweet finish Traffic is one of Atlanta's most dependable experiences.`), so the shop-voice cut, which only knew We/Our, let the prose ride into `Tasting notes:`. `cutValue` now also cuts at the no-boundary seam: a clause-starting function word, or a capitalised word directly after an all-lowercase one (`finish Traffic`), while a comma or capitalised neighbour (`Huila, Colombia`, `Stone Fruit`) does not cut. Fixture added from the live dev description; suite stays at 220; live on dev (`convex dev --once`).

### 2026-09-12 - dev run mh7ahzj41xhj1gwg2twqnsgrfs8e9enw (post-0fef273 verification)

First live run after the label-run fix (owner fired it, "a washed Colombian, easy drinking", no logs, no price/size caps; `gpt-5.6-luna`, status `ready`). The East Pole Traffic card quoted the mapped fact line — `Process: Washed. Variety: Caturra. Region: Huila, Colombia. Elevation: ~1,600 meters above sea level. Tasting notes: Dark Chocolate, Full-bodied, Sweet finish.` — which is both the #21 fix working end to end and the Firecrawl-style evidence shape reaching a card that previously would have quoted the flattened sheet with `New Column` in it. The model's comparison cited "Full-bodied" and "Dark Chocolate" from that line. Heart's Phono and Blossom's Deja Vu quoted catalog prose as before. One new finding: Deja Vu's unquoted "Also on the product page" passage is a customer review in first person ("I've had a tough time adjusting strength/grind...") presented under the roaster's-words heading; filed as an issue — the Firecrawl prompt says to ignore reviews, so this one leaked past extraction, and `SHOP_VOICE` only knows we/our, not I/my.

### 2026-09-12 - 0239eae

A code review of the #21 commits (done by reading the previous session's transcript and probing the new code with inputs taken from this repo's own docs) found that the seam cut added in a959b09 ran on every mapped value, not just the last one. Any function word inside an interior value truncated it: the Verve producer line held up in `.agents/docs/recommendations.md` as the target shape came out of the catalog path as `Producer: Smallholder outgrowers in.`, and a `NOTES This is a sweet cup` sheet produced `Tasting notes: This.`, both rendered in a blockquote under "Roaster's published words". The original `New Column` quote was at least verbatim; these were not. Prose can only be glued to the value that ends a run, since every other value ends where the next header begins, so `cutValue` now applies to that value alone and the rest are trimmed. Three smaller findings fixed in the same commit: `MASL` was eaten as an unknown header (`Elevation: 1900.`), the `We/Our` regex and the function-word list were two mechanisms for one cut, and a capitalised word followed by a lowercase function word (`Butterscotch Located in the ...`, live on La Reserva's page) showed no seam. Replaying the full dev `products` dump (2,560 rows) now gives clean fact lines for all nine label-run descriptions. Six fixtures added including one that pins the remaining blind spot (`Brown Sugar Traffic flows ...`) so a change there is deliberate; suite at 221. Not deployed yet: prod still serves the flattened sheet, and dev serves a959b09's truncation, so this should go out before the next live run.

### 2026-09-12 - prod deploy of 7be5a24 (label-run fix)

Pushed `0239eae` to dev (`convex dev --once`, 3.19s) and then to prod with `bun run deploy` (owner consent in session; 15.3s, functions plus a web rebuild with no frontend source change since `a08463d`, no schema or env change). Prod now carries the whole #21 line: flattened spec tables become `Label: value.` facts, and only the value that ends a run is checked for glued prose. Post-checks read-only: `/` and `/next-bag` 200, `healthCheck` OK, 19 of 20 prod sources carry a US/USD market confirmation. The twentieth is Passenger, still in `products_json` mode on prod with 179 consecutive failures because `drinkpassenger.com/products.json` is a 404; dev was flipped to `html` mode by hand weeks ago and prod never was, so Passenger is silently absent from every prod recommendation. Filed as #25 (`ready-for-human`: the prod row edit needs the owner, plus a `setSourceMode` operator mutation so the next time it is a logged CLI step). `recommendationRuns` on prod is still empty; the first prod recommendation remains to be recorded here.

### 2026-09-12 - prod Passenger row flip (owner) and the html-mode market gap (#26)

The owner set Passenger's prod crawl source to `html` mode in the dashboard (row `j972nxxzb8279njzgc76gps8n98dr69s`, #25). The next scheduler tick crawled successfully within six minutes: `watching`, `consecutiveFailures` reset from 179, 27 Passenger products on prod, and the crawl baselined itself because `lastSuccessAt` had never been set. The follow-up poll also exposed a code gap: `confirmShopMarket` is only called in the `products_json` path, so html-mode sources can never satisfy the eligibility gate `market.country === "US" && market.currency === "USD" && market.confirmedAt === lastSuccessAt` and are silently excluded from recommendations — on dev as well, where the row has been healthy and `market`-less for weeks. Passenger remains absent from Find my next bag until the code fix lands. Filed as #26 with the sketch (confirm inside `commitExtractedCatalog`, which is already an action, and pass it through `commitCatalog` like the products_json path does); `setSourceMode` remains the open code half of #25.

### 2026-09-12 - 8388cef

Fixed #26: html-mode crawls never called `confirmShopMarket`, so an html-mode source could not satisfy the recommendation eligibility gate (`market.country === "US" && market.currency === "USD" && market.confirmedAt === lastSuccessAt`) and was silently excluded from Find my next bag on both deployments. The confirmation now runs in `commitExtractedCatalog` against the roaster's `websiteUrl` (the action already loads the roaster for the baseline flag), stamping the commit's `fetchedAt`, which `finalizeCrawl` writes as `lastSuccessAt`. Missing Shopify globals or an unreachable homepage fail closed: no market, crawl still succeeds. The default fetch stub in `crawlSources.test.ts` returns a globals-free page, keeping all 56 pre-existing commit tests on their fail-closed shape; new cases assert the US/USD confirmation with `confirmedAt === lastSuccessAt` and both failure modes. Suite at 224. Left out deliberately: a "market unconfirmed" note on the source row (raised in #26) — it would warn hourly for any shop that legitimately lacks the globals, and the absent `market` field is already visible in the dashboard row; the eligibility gate stays fail-closed either way.

### 2026-09-15 - c7b18bc

Three result-card bugs from the third live dev run, all on `/next-bag`, the screen the demo turns on. **#22:** the Onyx card had shown `$6 USD / 85 g` because with no minimum bag size a 2 oz sample is the cheapest qualifying variant. The form now prefills 200 g (editable; cleared means any confirmed size, helper text says so) and the card reads `$6 USD · 2oz (85 g)` with the variant beside the price. The server's cheapest-qualifying rule is untouched. The payload builder moved into a pure function and `apps/web` got its first vitest setup (node environment, three cases: untouched, cleared, typed). **#23:** a Blossom Deja Vu customer review ("This is one of my 'go to' coffees... I've had a tough time") had appeared under "Roaster's published words" because it is on the page verbatim and `SHOP_VOICE` only knew we/our. A `REVIEWER_VOICE` marker (I, I've, I'm, my, me) now applies wherever shop voice does, including the label-run last-value cut. **#24:** Title Case spec sheets (`Region: ... Varietal: ... Process: ...`) had passed as one quotable sentence since #21 only counted all-caps headers. Three or more known colon-marked labels, case-insensitive, now trigger the same label-run mapping; unknown `Word:` cells drop with their values. Replaying the dev `products` export (2,570 rows) changed 201 descriptions, so this shape was far more common than the three the issue named, and surfaced three more to handle: two-word layout cells with the colon on the second word (`Relationship Since: 2020` had been leaving "Relationship" in the process value on ~40 rows), spaced colons (`Recipes : Espresso`), and a colon header after the last mapped value, which now bounds that value so the clause-seam cut stops eating `Finca Ojo de Agua`. Bare caps words do not bound (the Iyenga `AMCOS` acronym sits inside glued prose), so all-caps sheets read exactly as before. Backend suite 224 to 226, plus 3 web tests. Live on dev (`convex dev --once`, 3.5s, no schema change) and the dev `recommendationEvidence` cache purged (6 rows) since it held the review line for 24h. Not yet on prod.

### 2026-09-15 - prod deploy of c7b18bc (next-bag result cards)

Pushed `c7b18bc` to prod with `bun run deploy` (owner consent in session; 15.4s, functions plus a web rebuild, no schema or env change). Prod now serves the 200 g default and variant-beside-price card (#22), the reviewer-voice filter (#23) and the colon-label sheet mapping (#24). Purged `recommendationEvidence` on prod: 0 rows, confirming no recommendation has run there yet, so Task 2 of the handoff (the first recorded prod run) still stands. Post-checks read-only: `/` and `/next-bag` 200, `healthCheck` OK, the served `next-bag` chunk carries the new helper text. Issues #22, #23 and #24 closed with comments pointing at the commit. Deliberately parked for a later, broader pass over scraped-data cleanup: sentence-shaped notes values still lose everything after the first clause seam (`Tasting notes: Fragrance and aroma.`), the same cost the all-caps path already paid.

### 2026-09-15 - first prod recommendation, run ks752hap2q63hkq4b28ymwapa58efhbc

The owner fired the first real Find my next bag request through the signed-in prod UI, right after the `c7b18bc` deploy: preferences "Looking for something floral and of african origin", no logs selected, no notes, the new 200 g default left in place (`input.minGrams: 200`). Read back with `convex data recommendationRuns` on prod. Status `ready` after 21.4s (queued 21:57:50Z, ready 21:58:11Z), attempt 1. Model string returned by the Responses API and stored on the run: `gpt-5.6-luna`. Candidate pool: 20 lots across 10 roasters (Blossom, Coava, East Pole, Intelligentsia, La Colombe, Madcap, Merit, PT's, Ruby, Stumptown), drawn round-robin from the 20 US/USD-confirmed prod sources. Two Firecrawl enrichments spent: La Colombe "All Dark" produced three page passages including a server-labelled fact line (`Tasting notes: Chocolate Truffle, Toasted Walnut, Caramel.`), so the structured JSON path worked live on prod; the Blossom "Ardi" scrape cached zero passages (nothing new relative to the feed, or nothing readable), which is the empty-evidence path with its one-hour TTL. The model quoted three catalog passages and no Firecrawl line: Stumptown "Ethiopia Mordecofe" ($23 USD · 10.5oz, 299 g, "A complex and floral cup, sparkling with notes of nectarine, melon and jasmine."), Blossom "Decaf Ethiopia - Swiss Water Processed" ($21.50 · 12oz, 425 g) and Blossom "Ethiopia - Ardi - Natural" ($22.50 · 12 oz, 425 g), all labelled "A possible similarity" with a comparison sentence that survived `filterReason`. The card layout from #22 rendered as designed (variant beside price, 200 g helper text). Two honest notes for the demo: a decaf made the shortlist, which the rules allow (preference words rank, never filter, and "floral" matched its description), and the Firecrawl evidence was offered to the model but not chosen this time, so a video take that wants to open a Firecrawl source should pick a run where one is quoted, or show the unquoted "Also on the product page" bullets. This is the sponsor evidence the fit assessment said was missing: OpenAI and Firecrawl both ran on prod, in one request, from the public URL.

### 2026-09-15 - 0522e79

Task 3 of the polish handoff: Save, the one step of the spec §11 demo sequence (log, Find my next bag, save one, choose an alert, see a genuine alert, share a log) that did not exist. A `savedCoffees` table (`userId`, `productId`, `savedAt`, optional `fromRunId`; indexes `by_user_and_saved_at` and `by_user_and_product`) and `convex/savedCoffees.ts`. `save` and `unsave` take the user from `ctx.auth` only and are idempotent; `save` keeps `fromRunId` only when that run's `userId` is the caller, so nobody can pin a save to someone else's request. `mySavedProductIds` drives button state, `recentMine` returns the newest five plus a `more` flag for the home section, and `listMine` pages with `paginationOptsValidator` and hydrates each row with lot name, roaster, image and current stock (any available variant, bounded at `MAX_VARIANTS_PER_PRODUCT`; archived lots are never in stock). Saving is private: no email, no watch, and `logs.profile` is untouched, which a test pins by checking the profile payload carries no save. On the web, a Save/Saved toggle sits on every shortlist card (passing the run id), on the lot page beside "Log this lot", and on other people's log cards in the activity feed and profiles, since reading a log is the natural way a lot gets onto the list; it renders nothing signed out. The signed-in home gets a "Want to try" section above the roaster feed, `/saved` holds the full paginated list, and the header gains a Saved link. First save shows one toast: "Saved. Only you can see it; no email, no watch." Five convex-test cases (suite 226 to 231): sign-in required, save then unsave both idempotent, two users cannot see each other's saves, `fromRunId` recorded for the caller's own run and dropped for a foreign one, the home cap. Convex features: a new table with two compound indexes, `.unique()` on the per-user-per-lot index, `paginationResultValidator`, `useQuery` with `"skip"` signed out so the toggle never fires an unauthenticated query. Not yet deployed anywhere; this is the first schema change since the logs table, so it goes to dev first.

### 2026-09-15 - prod deploy of 0522e79 (Save)

Pushed `0522e79` to dev (`convex dev --once`, 3.3s) and then to prod with `bun run deploy` (owner consent in session; 16.9s, functions plus a web rebuild). First schema change since the logs table: the `savedCoffees` table and its two indexes, created empty so no backfill. No env change. Post-checks read-only: `/`, `/next-bag` and `/saved` 200, `healthCheck` OK, the table listed on prod, the served chunk carries "Want to try". The §11 demo sequence is now complete on the public URL: log, Find my next bag, save one, choose an alert, see an earlier alert, share a log. Design polish is deliberately deferred; the owner wants functionality nailed down first, then a lengthy design pass.

### 2026-09-15 - 7d0e305

Task 4 of the polish handoff, repo truthfulness: a judge opening the repo should not find screens that were promised and never built. `PRODUCT.md` was still the August build-spec capture (Add-roastery flow, Local scenes, Settings, prediction card in the screen inventory); it now describes the product as shipped, screen by screen, with a "Not in this release" list pointing at ADR-0004. That ADR records why Gate 3 of the revised spec shipped as Save only: privacy is all-or-nothing across every public query, coffee watches need a second relationship plus fanout dedup, and none of it appears in a three-minute video where "save it, then watch the roaster" already works. The header of this file now names the whole product (log, Find my next bag, save, watch) and carries a Demo line to fill at submission. Closed #14 (the OpenAI summary seam, superseded by verbatim roaster notes) and #12, whose last open item was directory search: `/roasters` gets a client-side filter over the loaded `listActive` result by name, city and state, every term must hit, accent and case insensitive, with four unit tests. Commented on #20 so it stands as the post-hackathon list. Deployed to prod with `bun run deploy` (owner consent in session; 15.0s, web rebuild only, no schema or env change); `/roasters` 200 and the served chunk carries the new empty-state copy.

### 2026-09-15 - extraction audit (handoff Task 1, investigation only)

Before the design pass, the owner asked for a deep look at the scraped data itself. No code changed and nothing deployed; the output is `.agents/research/extraction-audit.md`, a draft `adr/0005-one-extraction-pipeline.md` for the owner to decide, and issues #27 to #35. Method: all 20 seed feeds fetched live (4,367 items, every products.json page) and run through the real `parseProductsJson` and `classifyLot` with `bun run`, cross-checked against the dev export (2,570 products, 11,875 variants, 569 drop events), plus paired Firecrawl json scrapes on five product pages with the exact enrichment prompt and schema. The largest finding is not an extraction bug: Passenger has a normal Shopify feed at `www.drinkpassenger.com/products.json` (the apex is a headless front that 404s), so html mode, #25's 179 prod failures, and the hourly 50-credit Firecrawl crawl were all avoidable (#27). Next by demo visibility: `variants[].grams` is Shopify's shipping weight, wrong or zero on 21% of sized variants (East Pole "12 oz." is 397 g, every Blossom bag is 0 g and so can never be recommended, Verve's card reads "10 OZ (340 g)"); the variant name is right essentially always (#28). `origin` lands on 3 of 20 roasters, `process` on 5, `roastLevel` on 1, while 1,269 lots name their country in the title and 503 name their process (#30). `roasterNotes` swallows the clause after the list on 26 lots, keeps a Blossom emoji on 5, and misses Merit's `Prunes • Fig Danish • Nutmeg` bullet lines on 51 (#29). The classifier rejects Passenger's 35 "Archival Release" Freezer Friday lots and counts Merit's catalog two to three times through wholesale `vendor` duplicates; every other rejection across 1,620 items checked out (#31). Firecrawl extraction was byte-identical on elevation, process, region and tasting notes across all five paired runs and drifted on producer, sentences and one wrong-field roast level ("Espresso", on the page as recommended use), so the verbatim check needs a per-field shape check beside it before any page value lands in a column (#32). Dev drop events cluster in one-hour weekday windows (Verve Fri 07:00, 60 of 62; Proud Mary Wed midday; Ruby Wed 07:00; Sey Wed afternoon), which argues for a 15/30/60 cadence table and a raw-capture retention rule (#33), and "Check now" is designed around one shared crawl per source with per-user and per-source rate limits (#34). One correction to the handoff: Heart's facts are in `body_html` (`Location: Gedeb Elevation: 1900-2200m Varietals: Heirloom Process: Fully washed`), not in metafields.

### 2026-09-15 - 17abf56

First fix from the extraction audit, #27 (and the code half of #25). Passenger's `products.json` lives on `www.drinkpassenger.com`; the apex is a headless front that 404s, which is why the source had 179 straight failures on prod and a 50-credit-an-hour html crawl on dev. `fetchFirstFeedPage` in `extraction.ts` now retries page 1 on `www.` after an apex 404 (only a 404, only when the host has no `www.`), and whichever host answered fetches the later pages, confirms the market, and is written back to the roaster row by `recordFeedOrigin`, so a user-submitted headless shop gets the same treatment without an operator. Second finding while verifying: the `www.` homepage 301s to the headless apex, which has no `Shopify.currency` globals, so the market stayed unconfirmed and Passenger was still excluded from Find my next bag. `confirmShopMarket` now falls back to Shopify's storefront `/meta.json` (`{"country":"US","currency":"USD",...}`, checked on Sey, Onyx, Counter Culture and Stumptown too); the homepage globals stay primary so the other 19 confirm exactly as before. Two operator mutations beside `rebaselineSource`: `setSourceMode` (flips mode, zeroes the failure streak, health back to `watching`) and `purgeProductsByPrefix` (removes rows a retired source mode left behind, archiving any a taster logged, through the existing `purgeNonLot`). Seed sets Passenger's website to `www.`. Tests: four on the retry, one on the meta.json fallback plus three rejections (CA/CAD, AE/USD, non-JSON), one each on the two operator tools; suite 231 to 242. On dev (`convex dev --once`, 3.1s, no schema change): roaster row moved to `www.`, mode `html` to `products_json`, rebaselined, one crawl: `lastSuccessAt` set, `market` confirmed via `https://www.drinkpassenger.com/meta.json`, 261 current lots with description, image and tags (the audit's 260 plus one). The 29 bare html-era rows (`externalId` a product URL with a `?Size=` query) had no logs or saves, so they were purged along with their 55 bogus drop events instead of waiting out three strikes. Not yet on prod: the same row edit, mode flip and rebaseline are needed there after the deploy, with consent.

### 2026-09-15 - 904bc4b

#28, the audit's most visible defect: `variants[].grams` came straight from Shopify, and Shopify's `grams` is the shipping weight the roaster typed into the variant, not the bag. East Pole's "12 oz." carried 397, Verve's "10 OZ" 340, Sey's "125g" 454, Sweet Bloom's "2lb" 15,241, and every Blossom bag 0, which `meetsConstraints` (`grams > 0`) turned into "Blossom can never be recommended". `parseVariantGrams(name, optionValues, shopifyGrams)` in `extraction.ts` now reads the first size token in the variant title, then in `option1..3`, with the unit spellings the feeds actually use (`12oz`, `12 oz.`, `10 OZ`, `2.2 LBS`, `5lbs`, `250gms`, `250gm Wholebean`, `1 KILO`, `2 KILO`, `1 x 300 gram bag`), converts at 28.3495 g/oz and 453.592 g/lb, and multiplies by a pack count when the name has one (`2 x 250g bags` 500, `2x 8oz` 454, Counter Culture `12 - 4oz bags` 1,361, Onyx `10oz Case Pack (6)` 1,701). Shopify `grams` is the fallback only when no size is named and it is positive; a bare number (`10 Pack`, `1 Bag`), a gift-card amount or a grind word is not a size. Before deploying, a scratch script replayed all 20 live feeds from `/tmp/nouveau-audit/feeds` through the real `parseProductsJson`: 12,648 variants, 12,546 had a positive Shopify weight and 12,640 now have a size (Blossom 44 to 68 of 68, Merit 174 to 202, Verve 966 to 990), 6,966 values changed, and a listing of every change over 60% either way turned up one shape to fix (`12 - 4oz bags` had read as one 4 oz bag) and no wrong ones. 36 new cases, suite 242 to 278. On dev (`convex dev --once`, 3.2s), then one Blossom crawl by hand: all 43 Blossom variants now carry a size (`12 oz` 340, `2 lbs` 907, `5 lbs` 2,268). The other 19 self-heal on their next hourly crawl through `diffVariant`, which patches `grams` without an event, so no rebaseline. Not yet on prod.

### 2026-09-15 - b48d067

#29, the three `roasterNotes` defects. **Clause swallow.** The `notes of` and `flavors of` captures ran to the sentence end, so Passenger's "bittersweet chocolate and graham cracker, this classic Dark Roast tastes great on its own or with the addition of cream and sugar" was stored whole. The issue's first-conjunction rule wrecked Sey ("an intense and complex profile of blackberry lemonade, coffee blossom florals, and ripe nectarine" would have stopped at "lemonade"), so the seam is found differently: the list's final item starts at the last Oxford conjunction (", and C"), else at the bare "and", and after that point a comma followed by a clause word (this, making, reflecting, while, which, ...) or a capitalised subject ("..., Gradient is", "..., Even Keel is") ends the list. With no conjunction at all only the clause words cut, so "Cherry, Chocolate, Almond" stays whole, and a two-adjective final item ("and a rich, jam-like sweetness") is not a seam because "jam-like" is lowercase. **Prose `we taste`.** Blossom's "We taste dark chocolate and stewed blueberries, a perfect balance of refreshing and sweet . 🌑" was Ruby's line-bounded dash-list pattern applied to prose; a clause without a spaced-dash separator now stops at the sentence, every result drops its non-word tail, and the pattern only counts when the clause reads as a list, since Sey writes "one of the most dynamic we taste each season" and the old code stored "each season". Bonus from the replay: `\bwe\s+taste` matched inside "we tasted", which is where the eleven stored notes beginning with a bare "d " came from ("d from this year's Kenya harvest..."); a word boundary ends that. **Bullet lines.** Merit opens every lot with `Prunes • Fig Danish • Nutmeg` in its own block; a first line of three to six short items separated by `•`, `·` or `|` is now the notes before any lead-in is tried. Three items minimum, not the issue's two, because "Ethiopia • Guji" is as likely an origin line. Replay over the 20 live feeds through `stripHtml` (the stored `description` is flattened, so the dev export cannot show the bullet case): 4,391 items, notes on 1,414 before and 1,438 after, 56 bullet lines, punctuation or emoji tails 96 to 0, 35 gained (all Merit), 11 lost (all "we tasted" fragments). Over the dev export (2,571 rows) 86 values changed, 8 lost, all eight garbage. 18 new cases, suite 278 to 296. On dev (`convex dev --once`, 4.0s) plus Merit and Blossom crawls by hand; `lotCopy` is authoritative so the rest rewrite on the hourly crawl with no events and no rebaseline. Parked, as before: the `cutValue` seam in `recommendationRules.ts` for sentence-shaped `Tasting notes:` sheet values, a different code path. Not yet on prod.
