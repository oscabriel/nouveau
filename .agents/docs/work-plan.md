# Implementation order for the design pass

The work plan for the next sessions. Every batch below is decided in the ADRs (`adr/0011` through `adr/0017`, as amended by the 2026-09-20 grill, commit `59e618f`); nothing here re-decides anything. Sessions work from this doc: pick the next unblocked task, do the work, tick its box, and add a line to the progress log at the bottom. If a task reveals a decision that was not recorded, stop and record it in an ADR first.

The hackathon closes **2026-09-22 at noon Pacific**. The next-bag loop carries a deadline rule (ADR-0017): it ships only if it works on dev by Monday morning; otherwise the one-box page and cards ship on the current single-call pipeline and the loop lands after.

## Ground rules

- Backend changes are separate commits with tests before the UI that uses them (the design-pass rule). A new query field is a backend commit, never a drive-by inside a design change.
- Read `.agents/docs/convex-guidelines.md` before backend work and `.agents/docs/code-standards.md` before calling a batch done.
- Design changes never change behavior. Where a task does both, split the commits.
- Nothing pushed to `origin` yet; `main` sits at `59e618f`.

## Batch 0: foundations (no dependencies, start two sessions here)

Everything later is unblocked by this batch; it runs in parallel.

- [x] **Install `@convex-dev/agent`** (ADR-0017). Add the package and `app.use(agent, ...)` to `packages/backend/convex/convex.config.ts`, run `npx convex dev` once so `components.agent` generates. No code uses it yet. (`d55296d`)
- [ ] **Plate assets** (ADR-0012). The owner supplies the fresh hero export in `apps/web/public/`. Agent work: downscaled footer export of the branch, serving-size exports of the six details (they are 113 to 486 pixels on their long side), cream-fringe fix riding along in every cut. The 33.8 MB and 10.7 MB source-resolution files stay untracked. Agent half done (`e38eb91`, then moved to `apps/web/src/assets/coffea-arabica/*.webp` in `fc2974b`); only the hero export remains.
- [x] **Caveat, self-hosted** (ADR-0012). Download the font, add it to the web app per `DESIGN.md`'s font rule, and register it in the type scale as the caption face. (`02e02bf`, `@fontsource-variable/caveat`, registered as `font-caveat`)

## Batch 1: addressing (ADR-0011)

The foundation everything routes on. Backend first, routes after. No page changes yet.

- [x] **User handles, backend.** `users` gains `handle` (unique, indexed, checked against the reserved list) and `oldHandles` (array). Derivation from the Google display name at first sign-in with a numeric suffix on collision. `logs.profile` looks up by handle. Tests. (`8839eca`; old-handle storage amended to a `handleRedirects` table, recorded in ADR-0011, because an array index gives the lookup no path)
- [x] **Lot addressing, backend.** Index `products` on `(roasterId, handle)`. `lots.get` takes `(roaster slug, handle)`. At upsert, a duplicate handle gets the archived lot's last-seen year appended (`ethiopia-guji-2024`), detected through the new index, no scans. Every drop, catalog, saved and recommendation query returns the roaster slug and the handle with each row. Tests. (`e68479b`; `lots.addressById` added for the old-id redirect)
- [x] **Slug hygiene, backend.** Submission enforces slug uniqueness and reserved names; `seed.ts`'s `slugOf` and `submissions.ts`'s `normalizeShopUrl` collapse into one function. This closes the `eastpole` duplicate-slug throw. (`6d8dc96`; suffix resolution recorded in ADR-0011)
- [x] **Route tree.** New routes: `/roaster/$roaster`, `/roaster/$roaster/$lot`, `/drops`, `/settings` (+ existing `/settings/alerts`, new `/settings/appearance`, `/settings/account`), `/$user`, `/about`. Old paths (`/roasters/$slug`, `/lots/$lotId`, `/feed`, `/profile/$userId`, `/watches`, `/saved`) become real routes that redirect on mount and stay forever. Every `Link` that passed `lotId` passes the pair instead. (`183acab`; the roaster page is an index child, `roaster.$roaster.index.tsx`, so the lot route stays top-level without a parent layout; `/$user` adopts the canonical handle into the URL on mount)
- [ ] **`notifications.ts` writes the new paths** (lot links, mute link to `/settings/alerts`). Verify against a sent email template on dev. Backend half in (`8cb5475`); the dev email-template check waits for the route tree + a real SITE_URL.

## Batch 2: record and queries (ADRs-0014, 0016)

Backend commits the new surfaces' data before any UI mounts it.

- [x] **SCA wheel enum.** One module, shared by backend and web, holding the tasting-note vocabulary as a TypeScript enum with a Convex validator. Decide the cut of the wheel here (top-level categories, or top two levels; the full wheel is about a hundred leaves). `logs` gains the descriptor array, capped at four picks. Tests. (`0fff17d`; cut = top two levels, recorded in ADR-0016; the module is a literal-union type, not the `enum` keyword)
- [x] **Rated-tiles query.** Most recent rated logs joined to lot image, name, roaster slug and handle; pad with recent drops; dedupe by user when more than three exist; Shuffle draws from the most recent fifty with photos. Tests. (`ce7612d`; `tiles.ratedTiles`, shuffle seeded by the client so the draw is stable inside the query cache; drop padding reuses the feed's card hydration via `feed.dropCards`)
- [x] **`logs.profile` splits.** Public branch: logs only, never watches or the try list, enforced in the query. Owner branch adds watches (with health, mute) and the try list (with unsave, stock at last check). Tests. (`28cb33a`; `kind: "public" | "owner"` discriminates the branches; watch hydration moved to `watchCards`, saves to `savedCards`)
- [x] **Log removes save.** Logging a lot that is on the try list removes the save, with the undo toast still able to restore it. Tests. (`fa43bd8`; createLog deletes the save in the same transaction and returns its `fromRunId` for the undo)
- [x] **Roaster notes beside personal notes.** The lot page and profile queries return `roasterNotes` beside the log's picks where the UI needs both. (`712c6e7`; already in via the log-card hydration and the lot page's merged facts, tests pinned)

## Batch 3: shared table primitives (ADR-0015)

Built once, used by every table. Do before rebuilding any page that contains a table.

- [x] **Arrow cell.** The short right arrow replaces the circle on `DropTable`, joins the roasters directory and the lot catalog, links to the lot page everywhere.
- [x] **Floating hover image.** One element per table, about 280 pixels wide at 3:2, above-right of the pointer, clamped to the viewport, `pointer-events: none`, fine pointer only, keyboard-focus parity anchored to the row's leading edge. The in-cell slide and the fading row number go. Rows carry an image URL; the directory shows nothing.
- [x] **One link-class constant.** `navLinkClass` (header), `footerLink` (site-footer) and `capsLink` (roasters.index) collapse into one shared definition.

## Batch 4: chrome (ADRs-0012, 0013)

Depends on batch 1 (route names) and batch 0 (font, plate exports).

- [x] **Header.** Left: NOUVEAU, ROASTERS, DROPS. Right: ACTIVITY plus LOGIN signed out, ACTIVITY plus the handle signed in. The handle carries the dropdown: SETTINGS, and the LIGHT / DARK pair. One nav; below `md` the right group wraps instead of mounting twice. Sign out moves to `/settings/account`. (`195bd62`)
- [x] **Theme: system or inverted.** One switch flipping between the words LIGHT and DARK, default side from the OS, computed over `next-themes`' system value with a `matchMedia` listener. Mounted in `/settings/appearance` and the header dropdown, one stored state. Signed out: system, no control. The stored `light`/`dark` values are discarded on first load. (`c17ad96`)
- [x] **Footer.** Every route ends in it. Mirrored left links, BACK TO THE TOP center, ABOUT and GITHUB right, no user links. Below the links: the plate's parts with Caveat captions linking to Stamen, Gynoecium, Drupe (both berry images), Seed and Aril, and the large branch linking to Coffea arabica. Alt text on every image, not `aria-hidden`. (`045104a`)
- [x] **`/about`.** New route; copy from the hackathon log's "What it does" line and `PRODUCT.md`'s tone section. (`34f34d0`)

## Batch 5: the pages

Depends on batches 1-4. Roughly independent per bullet once batches 2 and 3 land; take them in any order after the header exists.

- [x] **Landing, one page both states** (ADR-0014). Hero (pre-rotated, leaves left, larger), NOUVEAU title, serif lede, primary slot = SIGN IN or FIND MY NEXT BAG by auth state, tiles (rated logs with black stars, pad, dedupe, Shuffle with underline instead of the dot), the drop table without Process and with MM.DD. The signed-in scaffold home is deleted. (`9ebc93e`; the pre-rotated hero export is still the owner's — the old plate renders until then)
- [x] **Roasters directory.** Your-status column (WATCH / WATCHING), no crawl-health column, no Status column at all signed out, arrow at row end. (ADR-0015)
- [x] **Roaster page.** Catalog rows end in the arrow; the inline log form and its tinted second row go; logging moves to the lot page. Health stays on the status line. (arrow and form removal already landed with batch 3; confirmed complete)
- [x] **`/drops`.** `DropTable` rows with the landing's filter tabs, per event with co-dropping variants grouped, event price with its strike on price drops, "from" minimum otherwise, no variant list, no Log action. Signed-in "Your roasters" tab with `feed.personalizedFeed` and delivery lines (pending, sent, delivered) under the rows.
- [x] **`/$user`.** Public view: logs with rating, review, structured notes, when. Owner view adds logs' edit and delete, watches with health and mute, the try list, the FIND MY NEXT BAG button, and the unhealthy-watch banner as one grey sentence. Watches and the try list never render for another viewer.
- [x] **`/settings/account`.** Handle edit (previous handle kept as redirect), name, sign out. `/settings/alerts` is unchanged. `/settings/appearance` is the theme mount from batch 4. (backend `updateMe` with the redirect row; page `settings.account.tsx`)
- [x] **Lot page.** Save, log, rate, REVIEW text, the four-pick tasting-notes picker beside the roaster's notes, edit and delete own logs. (picker + REVIEW + tastingNotes on create/edit; the page itself already mounted the form, save and edit/delete)

## Batch 6: the next-bag loop (ADR-0017)

The biggest item. Dependent on nothing else in this plan except batch 0, but gated by the Monday-morning rule. Start it early, in parallel with batches 1-5, and cut over only when the dev re-measurement passes.

- [x] **Jev request structuring.** Shipped, then removed the same day (ADR-0017 amendment): the loop is OpenAI only, and the model fills the search tool's typed args itself.
- [x] **Agent definition and tools.** `new Agent(components.agent, ...)`, model `openai.responses("gpt-5.6-luna")` with low reasoning effort and `store: false` as provider options on the stream call (chat completions rejects tools with reasoning for this model; the provider defaults `store: true`, which the no-persistence rule forbids). Tools as `createTool` wrappers over the internal queries the worker owns: search catalog, read lot facts (with the deferred Firecrawl read through the existing budget), check price and stock, read my logs (present only when the consent toggle is on).
- [x] **`submitPicks`.** The terminal tool; its execute validates ids against the catalog, re-checks stock and price, writes the validated result to the run document, and returns an error for the model to fix on failure. Nothing parsed from free text.
- [x] **Worker rewiring.** `recommendationWorker.run` stays the workpool entry: create the thread, call `agent.streamText` with `saveStreamDeltas: true`, `stopWhen: stepCountIs(n)`. Fresh thread per run, thread id stored on the run document. Quotas, watchdog, one-active-run and retry unchanged. The lexical score stays inside the search tool as its result ordering.
- [x] **Page.** One text box, the consent toggle (default off) and its grey sentence. Subscribes to the run document and the thread (`useUIMessages`, `stream: true`): steps as tool-call parts, queued and running states, then up to five simple cards ranked by the model's fit with the why from the validated result, then HOW IT LOOKED, collapsed. Thread queries authorize through run ownership.
- [ ] **Dev re-measurement.** Cost, latency, the 45-second HTTP timeout, the five-minute watchdog. This is the gate for the Monday-morning call. Needs one real signed-in run on dev (Google OAuth keeps it out of the headless round); the live step streaming and both themes on the real page are what to look at.

## Batch 7: finishing

- [ ] **`DESIGN.md` amendments.** Drop table loses Process and the year; header entry reflects the new right side and loses ModeToggle; the type scale gains Caveat; the no-cards rule gains its one exception; the Directory, Lot catalog and Index table entries follow ADR-0015; the marquee entry becomes the plate; the theme default becomes system-or-inverted (ADR-0013, line 127 says light); the filled ink block is shared with FIND MY NEXT BAG and the account Save (ADR-0014, line 198 says sign-in only).
- [ ] **`/activity` and the scaffold-era controls.** `/activity` has not had its design pass; `SaveButton`, the rounded chips and the amber stars predate the pass. Owner's call (2026-09-20): a batch 7 item, not shipped as is.
- [ ] **`hackathon.md`** updated once the loop ships (the "What it does" line and the OpenAI entry describe the single-call design today).
- [ ] **Redirect verification.** Old paths resolve; sent alert emails' links land; `/$user` for a changed handle lands on the old-handle redirect.
- [ ] **Both themes reviewed on every rebuilt page**, the standing rule of the design pass.

## Progress log

| Date | Batch | What landed |
| --- | --- | --- |
| 2026-09-20 | review fixes | The two design-pass reviews (`.agents/research/design-pass-review-2026-09-20.md`, `-part-2.md`) worked through, one fix per commit. Backend: the Jev why check now writes blanked selections back through `summarize` (S1, `52b41c8`); retired handles are not reserved and both claimers drop the dead redirect row (S5, `3efffdd`, ADR-0011 amended); `createLog` returns `removedSave` as an object so the undo toast fires for manual saves (S6, `5564ea2`); the picker lists floral and green/vegetative once (W2, `d372837`); flavour buckets match synonym lists and the bag ceiling reaches the first search (S3, S2, `7171710`); the consent gate has a test (`246d0df`); one `nextFreeSuffix` for handles, slugs and lot handles (`4223860`); the year-suffix rule reversed per the owner, the new lot takes the suffix and current colliders suffix too (S4, `6a73bb4`, ADR-0011 amended); `ratedTiles` dedupes only past three logs (`a84005b`). Web: the `/drops` tab strip outlives the Your roasters tab (W1, `b46c500`); Jev's reading leads the step list (`4389086`). Docs (`f5f548e`). `tsc` clean, Ultracite clean, 568 tests pass. Owner's other calls: ship S1 fixed before Monday (done); `/activity` and the scaffold controls are a batch 7 item. Still unseen live: the loop end to end with a blanked why, and the `/drops` tabs signed in. |
| 2026-09-20 | review fixes, backend standards | The standards and smell items from part one of the design-pass review (`.agents/research/design-pass-review-2026-09-20.md`, 3.1 and 3.2), one fix per commit. The `recommendationRuns` thread index is `by_user_id_and_thread_id` (`e26c042`); the handle, slug and lot-handle claims read one bounded range, `[base, base-:)` capped at `SUFFIX_SCAN_LIMIT`, and throw at the cap instead of risking a duplicate (`f49b2f2`); `readLotFacts` types through `readLot`'s returns validator and the action-to-action seam has its comment (`32582f9`); `RESERVED_ROUTES` is `as const` and the reserved checks are type guards (`7f12701`); `logs.profile` takes `address` (`abf51c4`); the tile pool carries each product it reads (`68033e1`). `tsc` clean, Ultracite clean, 571 tests pass. The index rename pushed to dev through the running `convex dev` watcher; it reaches prod with the next deploy. Left from the reviews: the web items in part two (3.1, 3.2) and the ADR-0016 profile-rows line. |
| 2026-09-20 | Jev out of next-bag | Owner's call: Find my next bag is the OpenAI sponsor showcase and should not lean on a second vendor. Backend (`bd3720f`): the worker no longer calls `structureRequest` or runs a seed search, the prompt is the request text alone and the model's first `searchCatalog` is the first step; `checkWhys`, `setStructured` and the blank-only path in `summarize` are gone; `latest` drops `structured`; the schema field stays optional and unwritten (no migration). ADR-0017 amended in the same commit. Web (`58fe211`): no synthetic Jev read line, no blanked-why text. Docs (`dd891ac`). `jev.ts` and the crawl-side readers untouched. `tsc` clean, Ultracite clean, 570 tests pass (two Jev tests out, one `summarize` test in). Deadline rule still binds. Not yet seen live: a run on dev without the seed search, which is also the check on whether the model fills the typed search args on its own. |
| 2026-09-20 | review fixes, web standards | The web items from part two of the design-pass review (`.agents/research/design-pass-review-2026-09-20-part-2.md`, 2.2, 2.3, 3.1, 3.2), one fix per commit. `DropTable` is generic over its row and hands `underRow` the table's own column count, so `drops.tsx` drops the `PersonalizedRow` cast and the literal `colSpan={10}`; `onlyTypes` is gone and the filtered `shown` rows go in (`844b0da`). `THEME_SIDES` lives once in `theme-switch.tsx` (`4e7caa7`). `/settings/account` prefixes the handle with `window.location.host` and `signOutConfirmed` is `signOutAndToast` (`b90d22b`). The footer's drupe images take the hover fade and all seven engravings load lazily (`272c313`). ADR-0016 gained an amendment reading "the latest rating" as per log, the default the implementation took (`e54a069`). `tsc` clean, Ultracite clean. Not done: the `MAX_STEPS` double `stopWhen` (part one 3.2, skipped by choice), the `handleRedirects` cap (note only), and the `/code-review` over `d6f1e63..HEAD`. |
| 2026-09-20 | 5 | Batch 5 complete. Owner feedback first: the signed-in header label is now the Google account's first name (fallback handle, never the id), sign out joined the dropdown, and a lazy `users.ensureMyHandle` backfills rows that predate ADR-0011 (the sign-in derivation fires for new sign-ins only, so those rows would never get a handle) — ADR-0012 amended. Then the pages: one landing for both states (`9ebc93e`) with the primary slot flipping SIGN IN / FIND MY NEXT BAG, rated tiles with black stars on the white chip and underlined toggles, drop table minus Process and MM.DD dates; directory Your-status column (ADR-0015); `/drops` rebuilt as a table with filter tabs, event prices with strike, "from" minimums (new `minPriceCents` on feed cards), and a signed-in Your roasters tab with delivery lines; `/$user` rebuilt as the personal record — public logs, owner watches with mute and unwatch, try list, FIND MY NEXT BAG, one grey attention sentence; `/settings/account` with name, handle edit (backend `updateMe` keeping the retired handle as a `handleRedirects` row, reclaim deleting it, 8 tests); the log form gained the four-pick SCA picker beside the roaster's notes and the freeform field is the REVIEW. `tsc` clean, Ultracite clean, 560 tests pass. Not yet seen live: signed-in pages under real OAuth, the handle backfill on the owner's legacy row, and both themes on every rebuilt page; the backend push to the deployment remains the owner's next `convex dev`. |
| 2026-09-20 | 4 | Batch 4 chrome complete (`c17ad96`, `195bd62`, `045104a`, `34f34d0`, `4090b91`, `70309bf`). Theme first: the preference is the relation system-or-inverted in the old `vite-ui-theme` slot, leftover absolute values discarded on read; next-themes keeps the system side under a private cache key and ThemeSync applies the relation before first paint and live on OS change; the LIGHT / DARK switch mounts on `/settings/appearance` and in the header's handle dropdown. Header: NOUVEAU, ROASTERS, DROPS left, ACTIVITY plus LOGIN or the handle right, one nav that wraps below `md`, dropdown holds the theme pair and SETTINGS, sign out leaves for `/settings/account`, ModeToggle deleted. Footer: marquee and its keyframes gone; link row mirrors the header with ABOUT and GITHUB right, no user links; the plate's six details with Caveat captions and Wikipedia links, the branch larger at the right; mounted once in the root layout so every route ends in it. `/about` rewritten from the hackathon What-it-does line and PRODUCT.md's tone. Reviewed live in both themes at 1440 and 390 on landing, roasters, drops, about, appearance; sign-out trim of batch 3's footer mounts done. Still unseen: the signed-in header (handle dropdown under real OAuth) and the theme switch's two mounts exercised by a signed-in user; the backend push to the deployment remains the owner's next `convex dev`. |
| 2026-09-20 | 6 | Batch 6 page (`1c816b1`): `/next-bag` rebuilt on the loop — one text box, consent toggle with its grey sentence, run + thread subscriptions, live step list from the streamed tool-call parts, up to five cards with the validated why under a caps OPENAI label, HOW IT LOOKED collapsed. New public `recommendationThreads.list` authorizes through run ownership; two tests pin it. Page reads verified at 1440 and 390 in both themes; the signed-in live run (step streaming against the real model) still needs a session on dev. |
| 2026-09-20 | 3 | Batch 3 table primitives done (`22b21a4`, `87056e5`). Backend first: lot rows now carry `imageUrl` (string or null) through `toLotRow`, pinned by a test. UI: `table.tsx` with `ArrowCell` (row-end arrow → the lot page; on the directory → the roaster page) and `TableHoverImage` (one fixed element per table, 280px at 3:2, above-right of the pointer and clamped to the viewport, fine pointer only, focus shows it at the row's leading edge, rows opt in via `data-image-url`, the directory mounts none). The drop table's circle, in-cell slide and fading number are gone; the catalog lost its Log/Close cell and inline log form to the arrow. The three caps-link constants collapsed into `lib/ui.ts` beside `headCell`/`bodyCell`. `tsc` clean, Ultracite clean, 553 tests pass. Live visual check of the floating image, arrow and both themes still needs a session on dev. |
| 2026-09-20 | - | All seven ADRs recorded (`f78ba65`) and every open question settled in the grill (`59e618f`). Nothing implemented. |
| 2026-09-20 | 0 | Agent component mounted (`d55296d`), Caveat self-hosted (`02e02bf`), plate serving exports defringed (`e38eb91`, now `src/assets/coffea-arabica/`). Owner still owes the pre-rotated hero. |
| 2026-09-20 | 1 | User handles backend (`8839eca`): handle derivation at sign-in, RESERVED_ROUTES, `logs.profile` resolving handle/redirect/legacy id. ADR-0011 amended for the `handleRedirects` table. |
| 2026-09-20 | 1 | Lot addressing backend (`e68479b`): `(roasterId, handle)` index, `lots.get` by pair, year-append on duplicate archived handles, handle + roaster slug on feed/log/saved/candidate rows. |
| 2026-09-20 | 1 | Slug hygiene backend (`6d8dc96`): shared `slugifyDomain`, submission-time slug claim with numeric suffix, reserved names enforced. ADR-0011 amendment recorded. All three batch-1 backend items are in. |
| 2026-09-20 | 1 | Alert email paths (`8cb5475`): lot → `/roaster/$slug/$handle`, roaster → `/roaster/$slug`, mute → `/settings/alerts`. Dev template verification still open. |
| 2026-09-20 | 1 | Route tree (`183acab`): all new addresses and redirect routes in, every link passing the (roaster slug, lot handle) pair, `tsc` clean and tests passing. Batch 1 done except the notifications email-template check on dev. |
| 2026-09-20 | 6 | Batch 6 backend complete (`676a99b`): the run is an agent loop on `@convex-dev/agent` — one-box input plus consent toggle, Jev request structuring into typed filters, search/read-facts/check-availability/read-my-logs tools over the existing internals, validating `submitPicks` terminal tool, fresh thread per run with streamed steps, Jev why check, summary line. The page, the dev re-measurement, and the Monday-morning gate remain. |
| 2026-09-20 | 6 | Follow-up manual runs after the tail fix, four new attempts: the tail fix verified live — three ready runs each end with the model's summary line in place, and the Jev why check fired for the first time ("most expensive coffees from california roasters": one pick kept with a careful why, one claim blanked by Jev, the drop note appended). The empty-picks path also exercised ("dark roast blend": says so instead of forcing matches). Latency 12.8-21.5 s across runs, steps well under the cap, cost under a cent per run, biggest run 17.6k input / 666 output tokens. The only failures in the table are the two pre-fix runs. Recall miss worth an owner look: the search tool's term filter demands the literal flavour word in the lot's text, so "dark roast blend" (Jev read it as chocolatey) found zero candidates every search while the catalog has chocolate-forward blends. Still never seen live: a consent-on run, a page read, and the retry button. |
| 2026-09-20 | 6 | First live signed-in run on dev worked end to end. Fix before it: the loop's model moved from `.chat()` to `.responses()` — OpenAI rejects function tools plus reasoning for gpt-5.6-luna over /v1/chat/completions — and the pinned low effort now rides as a provider option on the stream call (the agent component's `callSettings` merge drops provider options). First-run numbers: 12.8 s wall clock, 6 steps (2 SEARCH, 3 CHECKED, 1 HANDOFF) against MAX_STEPS 12, zero page reads, consent off so no log read, Jev structured the request ({flavour, origin}). Cost per run still needs the OpenAI dashboard. Found by that run and fixed: after `submitPicks` settled the run, the worker's `getRun` re-read (running-only) returned null and the tail exited silently, so the summary line and the why check never ran. New attempt-checked `readRun` for the tail, a test pinning the split, and the worker catch now logs the error name. |
| 2026-09-20 | 2 | Batch 2 backend complete. SCA wheel cut to its top two levels, `tasting.ts` shared by both apps, `logs.tastingNotes` capped at four picks (`0fff17d`). Rated-tiles query with drop padding and a seeded shuffle (`ce7612d`). `logs.profile` split into public and owner branches (`28cb33a`). Log-removes-save with the undo's `fromRunId` (`fa43bd8`). Roaster-notes-beside-picks pinned by tests (`712c6e7`). Functions pushed to dev. |
